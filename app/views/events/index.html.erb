<div id="k-body"><!-- content wrapper -->
  <div class="container"><!-- container -->
    <div class="row"><!-- row -->
      <%= render partial: 'layouts/search_form' %>
      <div class="k-breadcrumbs col-lg-12 clearfix"><!-- breadcrumbs -->
        <ol class="breadcrumb">
          <li><a href="<%= root_path %>">Home</a></li>
          <li class="active">Events</li>
        </ol>
      </div><!-- breadcrumbs end -->               
    </div><!-- row end -->
    <div class="row no-gutter"><!-- row -->
      <div class="col-lg-8 col-md-8"><!-- doc body wrapper -->
        <div class="col-padded"><!-- inner custom column -->
          <div class="row gutter"><!-- row -->
            <div class="col-lg-12 col-md-12">
              <h1 class="page-title">Events</h1><!-- category title -->
            </div>
          </div><!-- row end -->
          <div class="row gutter"><!-- row -->
            <div class="col-lg-12 col-md-12">
              <%- @events.each do |event| %>
	        <div class="up-event-wrapper"><!-- event summary -->
                  <h1 class="title-median">
		    <a href="<%= event_path(event) %>" title="Annual alumni game"><%= event.name %></a>
		  </h1>
                  <div class="up-event-meta clearfix">
	            <%- if event.is_single_day %>
                      <div class="up-event-date"><%= event.start_date %></div>
	              <%- if event.time.present? %>
	                <div class="up-event-time"><%= event.time %></div>
		      <%- end %>
	            <%- else %>
	              <div class="up-event-date"><%= event.start_date%></div>
	              <div class="up-event-date"><%= event.end_date%></div>
	            <%- end %>
                  </div>
                  <p>
                    <%= event.description.truncate(180) %>
		    <%- if event.description.length >  180 %>
	              <a href="<%= event_path(event) %>" class="moretag" title="read more">MORE</a> 
		    <%- end %>
                  </p>
                </div><!-- event summary end -->
	      <%- end %>
            </div>
          </div><!-- row end --> 
          <div class="row gutter"><!-- row -->
            <div class="col-lg-12">
              <%= paginate @events, theme: 'default_pagination' %>
            </div>
          </div><!-- row end -->                 
        </div><!-- inner custom column end -->
      </div><!-- doc body wrapper end -->
      <%= render partial: 'layouts/side_menu' %>
    </div><!-- row end -->
  </div><!-- container end -->
</div><!-- content wrapper end -->

